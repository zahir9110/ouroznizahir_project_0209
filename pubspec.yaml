name: benin_experience
description: "Application de tourisme culturel au Bénin"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # ============================================
  # 🎨 UI & RESPONSIVE
  # ============================================
  flutter_screenutil: ^5.9.0
  google_fonts: ^6.1.0
  
  # ============================================
  # 🏗️ ARCHITECTURE
  # ============================================
  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  dartz: ^0.10.1
  equatable: ^2.0.5
  
  # ============================================
  # 🌐 NETWORK
  # ============================================
  dio: ^5.4.0
  pretty_dio_logger: ^1.3.1
  
  # ============================================
  # 💾 STORAGE
  # ============================================
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  
  # ============================================
  # 🔥 FIREBASE
  # ============================================
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  firebase_storage: ^11.6.5
  firebase_messaging: ^14.7.10
  
  # ============================================
  # 🗺️ MAPS & LOCALISATION
  # ============================================
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  geolocator: ^10.1.0
  
  # ============================================
  # 🎯 UTILS
  # ============================================
  intl: ^0.18.1
  cached_network_image: ^3.3.1
  flutter_animate: ^4.3.0
  image_picker: ^1.2.1
  path_provider: ^2.1.2
  path: ^1.8.3
  shimmer: ^3.0.0
  uuid: ^4.5.2
  video_player: ^2.8.0
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  bloc_test: ^9.1.5
  mocktail: ^1.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/